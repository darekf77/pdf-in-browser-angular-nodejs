import { WorkerProcessClass } from './worker-process-class';
export declare class BootstrapWorker {
    static readonly READY_MESSAGE = "I am ready process worker";
    static bootstrap(classFN: typeof WorkerProcessClass, entities?: any[]): (port: any) => Promise<any>;
}
