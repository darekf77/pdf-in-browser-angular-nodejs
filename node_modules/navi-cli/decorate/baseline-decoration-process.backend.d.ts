import { Project } from '../project.backend';
import { Tools } from '../tools.backend';
export declare class BaselineDecorationProcess {
    private baselineProject;
    private moduleProject;
    private filesRelativePathes;
    static get quickFix(): {
        importAngular(pathToFile: string): void;
    };
    static getYeomanCommand(relativeFile: string, baselineProject: Project, moduleProject: Project): any;
    static getDecorateCommand(relativeFile: string, baselineProject: Project, moduleProject: Project): {
        command: string;
        newAbsolute: string;
        action: () => void;
        baselineProject: Project;
    };
    static getNewRelativePath(relativePathFromModule: string, moduleProject?: Project): string;
    static recognizeFileType(relativePath: string, moduleProject?: Project): Tools.NavigatorFileType;
    constructor(baselineProject: Project, moduleProject: Project, filesRelativePathes: string[]);
    run(): void;
}
