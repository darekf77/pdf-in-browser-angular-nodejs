/// <reference types="node" />
import { RunOptions } from 'tnp-core';
import { ProjectFeature } from './abstract/project-feature.backend';
export declare class ProjectHelpers extends ProjectFeature {
    private takenPorts;
    getFreePort(from?: number): Promise<number>;
    run(command: string, options?: RunOptions): {
        sync(): any;
        async(detach?: boolean): import("child_process").ChildProcess;
        asyncAsPromise(): any;
        unitlOutputContains(stdoutMsg: string | string[], stderMsg?: string | string[]): Promise<any>;
    };
}
