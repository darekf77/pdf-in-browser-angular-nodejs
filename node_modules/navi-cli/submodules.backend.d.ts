import { Project } from './project.backend';
import { Tools } from './tools.backend';
export declare class Submodules {
    private project;
    constructor(project: Project);
    cloneAll(): Promise<void>;
    clone(): Promise<void>;
    link(childProjectName: Tools.ModulesForDecoration, actionType?: 'clone-override' | 'soft-clone' | 'link'): Promise<void>;
}
