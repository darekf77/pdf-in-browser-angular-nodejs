export declare namespace ModelsSystem {
    interface RegisterServiceOptions {
        killAlreadyRegisterd?: boolean;
        actionWhenAssignedPort?: (itWasRegistered: boolean, registerdOnPort?: number) => any;
    }
    class SystemService {
        name: string;
        description?: string;
        constructor(name: string, description?: string);
    }
    interface WatchOptions {
        cwd: string;
        wait?: number;
    }
    interface PsListInfo {
        pid: number;
        ppid: number;
        memory: number;
        cpu: number;
        name: string;
        cmd: string;
    }
}
