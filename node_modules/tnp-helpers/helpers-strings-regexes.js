"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var tnp_core_1 = require("tnp-core");
var HelpersStringsRegexes = /** @class */ (function () {
    function HelpersStringsRegexes() {
    }
    HelpersStringsRegexes.prototype.escapeStringForRegEx = function (s) {
        return s.replace(/[-\/\\^$*+?.()|[\]{}]/g, '\\$&');
    };
    HelpersStringsRegexes.prototype.matchExactOnce = function (s, regex) {
        if (!tnp_core_1._.isString(s) || !tnp_core_1._.isRegExp(regex)) {
            return void 0;
        }
        var result = s.match(regex);
        if (tnp_core_1._.isNil(result)) {
            return void 0;
        }
        return result.length >= 1 ? tnp_core_1._.first(result) : void 0;
    };
    Object.defineProperty(HelpersStringsRegexes.prototype, "regex", {
        get: function () {
            return {
                /**
               * mathes
               * xxx.xxx.xxx.xxx
               * xxx.xxx.xxx.xxx:port
               * http://xxx.xxx.xxx.xxx:port
               * http://xxx.xxx.xxx.xxx
               * https://xxx.xxx.xxx.xxx:port
               * https://xxx.xxx.xxx.xxx   *
               */
                get forStringWithIpHost() {
                    var regex = /(http(s)?\:\/\/)?(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\.(25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)(\:[0-9]+)?/;
                    return regex;
                },
                /**
              * mathes
              * http://domain.com:port
              * http://domain.com
              * http://domain:port
              * http://domain
              * https://domain.com:port
              * https://domain.com
              * https://domain:port
              * https://domain
              */
                get forStringWithDomainHost() {
                    var regex = /(((http(s)?\:\/\/)?[a-zA-Z0-9][a-zA-Z0-9-]{1,61}[a-zA-Z0-9](?:\.[a-zA-Z]{2,})+(\:[0-9]+)?)|((http(s)?\:\/\/)[a-zA-Z0-9-]{1,61}))/;
                    return regex;
                }
            };
        },
        enumerable: true,
        configurable: true
    });
    return HelpersStringsRegexes;
}());
exports.HelpersStringsRegexes = HelpersStringsRegexes;
//# sourceMappingURL=helpers-strings-regexes.js.map