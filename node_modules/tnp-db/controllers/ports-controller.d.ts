import { BaseController } from './base-controlller';
import { PortsSet } from './ports-set.backend';
export declare class PortsController extends BaseController {
    update(): Promise<void>;
    private _manager;
    get manager(): Promise<PortsSet>;
    addExisted(): Promise<void>;
}
