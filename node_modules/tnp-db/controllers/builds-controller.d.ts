import { BaseController } from './base-controlller';
import { Project } from 'tnp-helpers';
import { BuildInstance } from '../entites/build-instance';
import { BuildOptions } from '../build-options';
export declare class BuildsController extends BaseController {
    /**
     * Update if proceses exists (by pid)
     */
    update(): Promise<void>;
    private getExisted;
    addExisted(): Promise<void>;
    killInstancesFrom(projects: Project[]): Promise<void>;
    add(project: Project, buildOptions: BuildOptions, pid: number, ppid: number): Promise<void>;
    distBuildFoundedFor(project: Project): Promise<BuildInstance>;
    appBuildFoundedFor(project: Project): Promise<BuildInstance[]>;
    getExistedByPid(pid: number): Promise<BuildInstance>;
    getExistedForOptions(project: Project, buildOptions: BuildOptions, pid?: number, ppid?: number): Promise<BuildInstance>;
}
