import { DbCrud } from '../db-crud';
export declare abstract class BaseController {
    protected crud: DbCrud;
    constructor(crud: DbCrud);
    abstract addExisted(): Promise<any>;
    abstract update(): Promise<any>;
}
