import { BaseController } from './base-controlller';
import { ProjectInstance } from '../entites';
export declare class ProjectsController extends BaseController {
    update(): Promise<void>;
    private recognized;
    addExisted(): Promise<void>;
    addIfNotExists(projectInstance: ProjectInstance): Promise<boolean>;
    discoverProjectsInLocation(location: string, searchSubfolders?: boolean): Promise<void>;
}
