import { Project } from 'tnp-helpers';
import { BuildInstance } from './build-instance';
import { DBBaseEntity } from './base-entity';
import { ConfigModels } from 'tnp-config';
export declare class IDomainInstance {
    address: string;
}
export declare class DomainInstance extends DBBaseEntity implements IDomainInstance {
    address: string;
    isEqual(anotherInstace: DomainInstance): boolean;
    constructor(address?: string);
    declaredIn: {
        project: Project;
        environment: ConfigModels.EnvironmentName;
    }[];
    get activeFor(): BuildInstance;
}
