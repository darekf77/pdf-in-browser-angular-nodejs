import { DBBaseEntity } from './base-entity';
export declare class CommandInstance extends DBBaseEntity {
    command?: string;
    location?: string;
    isBuildCommand: boolean;
    isEqual(anotherInstace: CommandInstance): boolean;
    constructor(command?: string, location?: string, isBuildCommand?: boolean);
    static fixedCommand(command: string): string;
    get shortCommandForLastCommand(): string;
    static from(location: string): {
        command(command: string): CommandInstance;
    };
}
