export declare abstract class DBBaseEntity<T = any> {
    abstract isEqual(anotherInstace: DBBaseEntity<any>): boolean;
    entityName(): string;
    static entityNameFromClassName(className: string): string;
}
