import { TnpDB } from '../wrapper-db.backend';
export declare class DBMonitCommands {
    private db;
    constructor(db: TnpDB);
    start(): Promise<void>;
    counter: number;
    private repeat;
}
