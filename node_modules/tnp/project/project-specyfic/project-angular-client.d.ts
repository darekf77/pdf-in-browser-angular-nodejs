import { Project } from '../abstract';
import { BuildOptions } from 'tnp-db';
/**
 * DO NOT USE environment variables in this project directly
 */
export declare class ProjectAngularClient extends Project {
    buildLib(): Promise<void>;
    get isEjectedProject(): boolean;
    filesTemplates(): string[];
    projectSpecyficFiles(): string[];
    preventWarningTypescirptMismatch(): void;
    startOnCommand(args: string): string;
    buildApp(watch: boolean, prod: boolean, port?: number, baseHref?: string, flags?: string[], args?: string): Promise<void>;
    buildSteps(buildOptions?: BuildOptions): Promise<void>;
}
