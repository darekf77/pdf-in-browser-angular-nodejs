import { CodeCut } from 'morphi';
import { Models } from 'tnp-models';
import { Project } from '../../abstract';
import { BuildOptions } from 'tnp-db';
export declare class ExtendedCodeCut extends CodeCut {
    protected cwd: string;
    /**
     * it may be not available for global, for all compilatoin
     */
    private project;
    private compilationProject;
    private buildOptions;
    sourceOutBrowser: string;
    browserCodeCut: any;
    constructor(cwd: string, filesPathes: string[], options: Models.dev.ReplaceOptionsExtended, 
    /**
     * it may be not available for global, for all compilatoin
     */
    project: Project, compilationProject: Project, buildOptions: BuildOptions, sourceOutBrowser: string);
    file(absolutePathToFile: any): any;
}
