import { BrowserCodeCut } from 'morphi';
import { Models } from 'tnp-models';
import { Project } from '../../abstract';
import { BuildOptions } from 'tnp-db';
export declare class BrowserCodeCutExtended extends BrowserCodeCut {
    private project?;
    private compilationProject?;
    private buildOptions?;
    private sourceOutBrowser?;
    get allowedToReplace(): import("tnp-config").ConfigModels.CutableFileExt[];
    debug(fileName: string): void;
    handleTickInCode(replacement: string): string;
    handleOutput(replacement: string, ext: Models.other.CutableFileExt): string;
    constructor(absoluteFilePath: string, project?: Project, compilationProject?: Project, buildOptions?: BuildOptions, sourceOutBrowser?: string);
    afterRegionsReplacement(content: string): string;
    private replaceHtmlTemplateInComponent;
    private replaceCssInComponent;
    private replaceSCSSInComponent;
    private findReplacements;
    replaceFromLine(pkgName: string, imp: string): void;
    replaceRegionsForIsomorphicLib(options: Models.dev.ReplaceOptionsExtended): this;
    replaceRegionsWith(stringContent?: string, replacementPatterns?: any[], replacement?: string, ext?: Models.other.CutableFileExt): any;
    saveOrDelete(): void;
}
