import type { Project } from '../../abstract/project/project';
/**
 * app - angular app
 * - src (angular-lib)
 * lib
 *  - src (isomorphic-lib) -> lib-name/src
 *  - components (angular-lib)   -> lib-name/components
 * tmp-src
 *  - isomorphic-lib
 *  - angular-lib
 *  - site isomorphic-lib
 *  - site angular-lib
 * custom/app
 * custom/lib
 *  - src (isomorphic-lib)
 *  - components (angular-lib)
 */
export declare type ModType = 'app' | 'lib' | 'custom/app' | 'custom/lib' | 'tmp-src' | 'tmp-src-for';
export declare type CheckType = 'standalone' | 'baseline' | 'site';
export declare type ImpReplaceOptions = {
    debugMatch?: boolean;
    debugNotMatch?: boolean;
    relativePath: string;
    project: Project;
    method: CheckType;
    modType: ModType;
    partsReplacementsOptions?: {
        replaceWhole?: boolean;
    };
    urlParts: (string | string[])[] | RegExp;
    notAllowedAfterSlash?: (string | string[])[];
    partsReplacements: (string | string[])[];
    name: string;
    input: string;
};
