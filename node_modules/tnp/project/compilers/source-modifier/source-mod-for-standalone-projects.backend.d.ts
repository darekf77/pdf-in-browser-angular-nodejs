import { Project, FeatureCompilerForProject } from '../../abstract';
import { ModType } from './source-modifier.models';
import { Models } from 'tnp-models';
export declare class SourceModForStandaloneProjects extends FeatureCompilerForProject<Models.other.ModifiedFiles, Models.other.ModifiedFiles> {
    project: Project;
    constructor(project: Project);
    protected get foldersSources(): string[];
    protected get foldersCompiledJsDtsMap(): string[];
    protected process(input: string, relativePath: string): string;
    processFile(relativePath: string, files: Models.other.ModifiedFiles, source?: 'tmp-src-for'): boolean;
    protected getModType(project: Project, relativePath: string): ModType;
    protected mod3rdPartyLibsReferces(input: string, modType: ModType, relativePath: string): string;
}
