import { Project, FeatureCompilerForProject } from '../../abstract';
export declare class EntitesGenerator extends FeatureCompilerForProject {
    project: Project;
    constructor(project: Project);
    private entityRepo;
    private entitesTemplateExportImport;
    private entitesArray;
    protected generateEntityTs(cwd: string, isSiteInStrictMode?: boolean): void;
    private repositoriesTemplateExportImport;
    private entitesTemplateDB;
}
