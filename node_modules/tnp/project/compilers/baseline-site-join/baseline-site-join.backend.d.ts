import { FeatureCompilerForProject } from '../../abstract';
import { IncCompiler } from 'incremental-compiler';
import { Project } from '../../abstract/project';
import { FilesJoinActions } from './files-join-actions.backend';
export declare class BaselineSiteJoin extends FeatureCompilerForProject {
    project: Project;
    constructor(project: Project);
    asyncAction(event: IncCompiler.Change): Promise<void>;
    /**
     * check at start if files in (src|componets) should exisit in site project
     */
    private get syncActionAlreadyMergedFiles();
    syncAction(filesAbsolutePathes: string[]): Promise<void>;
    private returnRelativePathIfAllowed;
    private resolveRelativePath;
    /**
     * relativeBaselineCustomPath -> example src/apps/auth/AuthController.ts
     */
    private merge;
    private replacePathFn;
}
export interface BaselineSiteJoin extends Partial<FilesJoinActions> {
}
