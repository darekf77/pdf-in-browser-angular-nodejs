import { Project } from './project';
import { IncCompiler } from 'incremental-compiler';
export declare abstract class FeatureCompilerForProject<RES_ASYNC = any, RES_SYNC = any, ADDITIONAL_DATA = any> extends IncCompiler.Base<RES_ASYNC, RES_SYNC, ADDITIONAL_DATA> {
    project: Project;
    constructor(project: Project, options: IncCompiler.Models.BaseClientCompilerOptions, allowFolderOutSideProject?: boolean);
    protected get notAllowedToWachFiles(): any[];
}
