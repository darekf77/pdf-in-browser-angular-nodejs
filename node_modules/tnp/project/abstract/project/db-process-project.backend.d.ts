import type { Project } from './project';
export declare abstract class DbProcessProject {
    hasParentWithSingularBuild(this: Project): Promise<boolean>;
    isSingularBuild(this: Project): Promise<boolean>;
}
