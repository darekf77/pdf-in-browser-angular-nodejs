import { Project } from './project';
import { Models } from 'tnp-models';
export declare abstract class VscodeProject {
    get vscodeFileTemplates(this: Project): string[];
    recreateCodeWorkspace(this: Project): void;
    private get nameOfCodeWorkspace();
    openInVscode(this: Project): void;
    temlateOfTasksJSON(this: Project, currentWorkspaceConfig: Models.env.EnvConfig): string;
    temlateOfLaunchJSON(this: Project, currentWorkspaceConfig: Models.env.EnvConfig): string;
}
