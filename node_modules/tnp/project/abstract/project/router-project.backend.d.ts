export { ChildProcess } from 'child_process';
import { Project } from './project';
export declare abstract class RouterProject {
    protected __defaultPort: number;
    get port(this: Project): number;
    protected startOnCommand(this: Project, args: string): string;
    routerTargetHttp(this: Project): any;
    setDefaultPort(this: Project, port: number): void;
    getDefaultPort(this: Project): any;
    setDefaultPortByType(this: Project): void;
    /**
     * Start server on top of static build
     * @param port
     */
    start(this: Project, args?: string): Promise<void>;
}
