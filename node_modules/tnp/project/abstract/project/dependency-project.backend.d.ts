import { ProjectBuild } from 'tnp-helpers';
import { Project } from './project';
export declare abstract class DependencyProject {
    projectsInOrderForChainBuild(this: Project, targets?: Project[]): Project[];
    /**
     * Only for continaer
     */
    projectsFromArgs(this: Project, args: string, argsChangeFn: (newArgs: any) => void): {
        project: Project;
        copyto: Project[];
    }[];
    projectsInOrderForBuild(this: Project, buildAppsForProjects: boolean): ProjectBuild[];
    get sortedRequiredWorkspaceDependencies(this: Project): Project[];
    libsForTraget(this: Project, project: Project): Project[];
}
