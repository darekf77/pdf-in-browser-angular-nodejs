import { BuildOptions } from 'tnp-db';
import type { Project } from './project';
export declare abstract class BuildableProject {
    _buildOptions?: BuildOptions;
    get availableIsomorphicPackagesInNodeModules(this: Project): string[];
    /**
     * return copyto array with absulute pathes
     */
    selectProjectToCopyTO(this: Project, buildOptions?: BuildOptions): Promise<string[]>;
    get buildOptions(): Project;
    set buildOptions(this: Project, v: Project);
    protected buildSteps(buildOptions?: BuildOptions): Promise<void>;
    get allowedEnvironments(this: Project): any;
    private selectAllProjectCopyto;
    isReadyForTarget(this: Project, target: Project): boolean;
    build(this: Project, buildOptions?: BuildOptions): Promise<void>;
}
