import { Project } from '../../abstract';
import { Models } from 'tnp-models';
import { NodeModulesCore } from './node-modules-core.backend';
export declare class NodeModulesBase extends NodeModulesCore {
    /**
     * Copy (just linke npm install) all package from
     * source project node_modules to itself
     * @param source project - source of node_modules
     * @param triggerMsg reason to copy packages
     */
    copyFrom(source: Project, triggerMsg: string): Promise<void>;
    private get copyBin();
    /**
     *  copy package to project
     * @param pkg
     * @param options
     */
    copy(pkg: string | Models.npm.Package, options?: {
        override?: boolean;
        linkOnly?: boolean;
    }): {
        to(destination: Project): Promise<void>;
    };
}
