import { Project } from '../../abstract';
import { Models } from 'tnp-models';
export declare namespace CopyMangerHelpers {
    function filterDontCopy(basePathFoldersTosSkip: string[], project: Project): (src: string, dest: string) => boolean;
    function filterOnlyCopy(basePathFoldersOnlyToInclude: string[], project: Project): (src: string, dest: string) => boolean;
    function executeCopy(sourceLocation: string, destinationLocation: string, options: Models.other.GenerateProjectCopyOpt, project: Project): void;
}
