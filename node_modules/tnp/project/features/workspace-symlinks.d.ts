import { FeatureForProject } from '../abstract';
export declare class WorkspaceSymlinks extends FeatureForProject {
    private get linkInside();
    remove(triggeredMsg: string): void;
    add(triggeredMsg: string): void;
}
