import { FeatureForProject } from '../abstract';
import { Models } from 'tnp-models';
export declare type OverridePacakge = {
    [name: string]: string | null;
};
export declare type PackageType = Pick<Models.npm.Package, 'name' | 'version'>;
export declare class SmartNodeModules extends FeatureForProject {
    private static _prepared;
    private getAndCreateTempProjForPackage;
    private get containerCore();
    /**
     * path for smart node_modules for whole project
     */
    get path(): string;
    pathFor(packageName?: string): string;
    get exists(): boolean;
    private get toOverride();
    private handlePackagesOverride;
    install(action?: 'install' | 'uninstall', ...packages: PackageType[]): void;
}
