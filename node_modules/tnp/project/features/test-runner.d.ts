import { FeatureForProject } from '../abstract';
export declare type TestType = 'unit' | 'integration' | 'e2e';
export declare class TestRunner extends FeatureForProject {
    fileCommand(files: string[]): string;
    start(files?: string[], watchMode?: boolean, debugMode?: boolean): Promise<void>;
    startAndWatch(files?: string[], debug?: boolean): Promise<void>;
}
