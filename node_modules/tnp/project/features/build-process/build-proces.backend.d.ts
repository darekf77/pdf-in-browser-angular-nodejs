import { FeatureForProject, Project } from '../../abstract';
import { Models } from 'tnp-models';
export declare class BuildProcess extends FeatureForProject {
    static prepareOptionsBuildProcess(options: Models.dev.StartForOptions, project: Project): Models.dev.StartForOptions;
    startForLibFromArgs(prod: boolean, watch: boolean, outDir: Models.dev.BuildDir, args: string): Promise<void>;
    /**
     * prod, watch, outDir, args, overrideOptions
     */
    startForLib(options: Models.dev.StartForOptions, exit?: boolean): Promise<void>;
    startForAppFromArgs(prod: boolean, watch: boolean, outDir: Models.dev.BuildDir, args: string): Promise<void>;
    startForApp(options: Models.dev.StartForOptions, exit?: boolean): Promise<void>;
    private mergeNpmPorject;
    private build;
}
