import { Models } from 'tnp-models';
import { NpmPackagesCore } from './npm-packages-core.backend';
export declare class NpmPackagesBase extends NpmPackagesCore {
    get useSmartInstall(): boolean;
    installProcess(triggeredMsg: string, options?: Models.npm.NpmInstallOptions): Promise<void>;
}
