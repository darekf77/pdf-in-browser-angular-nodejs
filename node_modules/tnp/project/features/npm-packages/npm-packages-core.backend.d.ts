import { FeatureForProject } from '../../abstract';
import { Models } from 'tnp-models';
export declare class NpmPackagesCore extends FeatureForProject {
    protected get emptyNodeModuls(): boolean;
    package(pacakgeName: string): {
        isSatisfyBy(versionOrRange: string): boolean;
        isNotSatisfyBy(versionOrRange: string): boolean;
        readonly version: string;
        readonly location: string;
        readonly exists: boolean;
    };
    protected actualNpmProcess(options?: Models.npm.ActualNpmInstallOptions): void;
    private smoothInstallPrepare;
}
