import { Project } from '../../abstract';
import { Models } from 'tnp-models';
import { PackageJsonCore } from './package-json-core.backend';
export declare class PackageJsonBase extends PackageJsonCore {
    readonly project: Project;
    private reasonToHidePackages;
    private reasonToShowPackages;
    private readonly coreCategories;
    constructor(options: {
        data: Object;
        location?: string;
        project?: Project;
    });
    save(reasonToShowPackages: string): void;
    showDeps(reasonToShowPackages: string): void;
    hideDeps(reasonToHidePackages: string): void;
    updateHooks(): void;
    private prepareForSave;
    linkTo(destination: string): void;
    removeDependencyAndSave: (p: Models.npm.Package, reason: string) => void;
    setDependencyAndSave: (p: Models.npm.Package, reason: string) => void;
    /**
     * Look all package.json dependencies and check if version range
     * of dependency is satisfy
     * @param dependency
     */
    checDepenciesAreSatisfyBy(dependency: Project): boolean;
    reset(): void;
    setCategoryFor(pkg: Models.npm.Package): Promise<void>;
    updateFrom(locations: string[]): void;
}
