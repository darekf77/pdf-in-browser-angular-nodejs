import { PackageJsonBase } from './package-json-base.backend';
import type { Project } from '../../abstract';
export declare class PackageJSON extends PackageJsonBase {
    static fromProject(project: Project): PackageJSON;
    static fromLocation(location: string, project?: Project, warings?: boolean): PackageJSON;
}
