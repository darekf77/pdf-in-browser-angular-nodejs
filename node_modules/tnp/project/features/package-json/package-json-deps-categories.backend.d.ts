import { FeatureForProject } from '../../abstract';
import { Models } from 'tnp-models';
import { ConfigModels } from 'tnp-config';
export declare type GetPkgType = {
    category: ConfigModels.CoreLibCategory;
    version: string;
};
export declare class PackageJsonDepsCoreCategories extends FeatureForProject {
    protected s_onlyFor: string;
    protected s_common: string;
    private get all();
    private for;
    private get commonForAllLibTypes();
    private removeAllWithName;
    getBy(name: string): GetPkgType;
    setWizard(pkg: Models.npm.Package): Promise<void>;
    set(pkg: Models.npm.Package, type: ConfigModels.CoreLibCategory): void;
}
