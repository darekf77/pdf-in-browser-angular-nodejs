export declare class RenameRule {
    from: string;
    to: string;
    org: {
        from: string;
        to: string;
    };
    constructor(from: string, to: string);
    applyTo(s: string): boolean;
    toString: () => string;
    replace(orgString: string): string;
}
export declare class FilesRenaming {
    static start(aa: string): void;
    rules: RenameRule[];
    private argString;
    constructor(argString: string);
    changeFiles(files: string[], startProcessAgain: (newFolder: string) => any, isFirstCall?: boolean): any;
    changeContent(files?: string[]): void;
}
