import { FeatureForProject, Project } from '../../abstract';
export declare type CleanType = 'all' | 'only_static_generated';
export declare type InitOptions = {
    watch: boolean;
    watchOnly?: boolean;
    alreadyInitedPorjects?: Project[];
    initiator?: Project;
};
export declare class FilesStructure extends FeatureForProject {
    findBaselines(proj: Project, baselines?: Project[]): Project[];
    private fixOptionsArgs;
    private get taskNames();
    struct(args?: string): Promise<void>;
    init(args: string, options?: InitOptions): Promise<void>;
    recreateSiteChildren(): Project[];
    private recrusiveOperation;
    reset(options?: {
        recrusive: boolean;
    }): Promise<void>;
    clear(options?: {
        recrusive: boolean;
    }): Promise<void>;
    private resolveArgs;
    resetFromArgs(args: any): Promise<void>;
    clearFromArgs(args: any): Promise<void>;
}
