import { FeatureForProject } from '../../abstract';
export declare class FilesTemplatesBuilder extends FeatureForProject {
    get files(): string[];
    rebuild(soft?: boolean): void;
    rebuildFile(filetemplateRelativePath: any, soft?: boolean): void;
    private processFile;
}
