import { FeatureForProject } from '../../abstract';
export declare class FilesFactory extends FeatureForProject {
    createFile(pathToFile: string, content?: string | JSON): void;
    create(...pathes: string[]): {
        file(content: string): void;
    };
    createModel(relativePath: string, name: string): void;
    private createEntity;
    private createController;
}
