import { FeatureForProject } from '../abstract';
export declare class ProxyRouter extends FeatureForProject {
    private static takenPorts;
    static getFreePort(from?: number): Promise<number>;
    private getProjectFrom;
    activateServer(onServerReady: (serverPort?: number) => void): Promise<void>;
    private getTarget;
    private server;
}
