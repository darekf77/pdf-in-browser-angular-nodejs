import { FeatureForProject } from '../abstract';
export declare class GitActions extends FeatureForProject {
    private before;
    private cloneUnexistedProjects;
    private getLinkedPorjectsAndChildrens;
    private repeatMenu;
    push(commitMessage?: string, force?: boolean): Promise<void>;
    pull(): Promise<void>;
}
