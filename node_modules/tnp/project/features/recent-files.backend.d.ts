import { FeatureForProject } from '../abstract/feature-for-project';
export declare type RecentFilesJson = {
    recentOpenProjects: string[];
};
export declare class RecentFilesForContainer extends FeatureForProject {
    private constructor();
    private get defaultValue();
    saveActiveProjects(override?: boolean): Promise<void>;
    private readConfig;
    private get localRecentProjects();
    openRecent(): void;
    setFrom(args: string, override?: boolean): void;
}
