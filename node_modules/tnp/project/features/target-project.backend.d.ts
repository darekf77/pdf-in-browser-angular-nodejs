import { FeatureForProject } from '../abstract/feature-for-project';
export declare class TargetProject extends FeatureForProject {
    get exists(): boolean;
    private get all();
    update(): Promise<void>;
}
