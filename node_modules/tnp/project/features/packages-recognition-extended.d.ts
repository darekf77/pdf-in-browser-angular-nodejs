import { PackagesRecognition } from 'morphi';
import { Project } from '../abstract';
export declare class PackagesRecognitionExtended extends PackagesRecognition {
    static fromProject(project: Project): PackagesRecognitionExtended;
    start(force: boolean, reasonToSearch: string): void;
    checkIsomorphic(node_modules: string, packageName: string): boolean;
}
