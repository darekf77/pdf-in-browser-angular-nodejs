import { OnInit, ChangeDetectorRef } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { FormlyFieldConfig } from '@ngx-formly/core';
import { MatDialog } from '@angular/material/dialog';
import { PROJECT } from '../../../PROJECT';
import { BaseComponent } from 'tnp-helpers';
import { TnpProjectTabIndex } from '../tabs-menu-tnp-project';
export declare abstract class BaseItemStepperProcessBuildComponent extends BaseComponent implements OnInit {
    private _formBuilder;
    protected cd: ChangeDetectorRef;
    matDialog: MatDialog;
    formGroup: FormGroup;
    model: PROJECT;
    abstract tabNumber(): TnpProjectTabIndex;
    abstract tabSelectedAction(tabIndex?: number): any;
    abstract formValueChanged(): any;
    fields: FormlyFieldConfig[];
    constructor(_formBuilder: FormBuilder, cd: ChangeDetectorRef, matDialog: MatDialog);
    ngOnInit(): void;
}
