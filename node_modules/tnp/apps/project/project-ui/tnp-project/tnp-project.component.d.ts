import { OnInit, EventEmitter } from '@angular/core';
import { FormGroup, FormBuilder } from '@angular/forms';
import { StepperSelectionEvent } from '@angular/cdk/stepper';
import { TnpProjectTabIndex } from './tabs-menu-tnp-project';
import { PROJECT } from '../../PROJECT';
import { BaseComponent } from 'tnp-helpers';
export declare class TnpProjectComponent extends BaseComponent implements OnInit {
    private formBuilder;
    TnpProjectTabIndex: typeof TnpProjectTabIndex;
    constructor(formBuilder: FormBuilder);
    expandedChild: string;
    showChildren: boolean;
    opened: EventEmitter<any>;
    closed: EventEmitter<any>;
    expanded: boolean;
    isChild: boolean;
    model: PROJECT;
    environmentFormGroup: FormGroup;
    buildFormGroup: FormGroup;
    testFormGroup: FormGroup;
    serveFormGroup: FormGroup;
    releaseFormGroup: FormGroup;
    closeChild(name: string): void;
    toogleChildren(): void;
    get keyForProject(): string;
    ngClass(index: number): {
        bold: boolean;
    };
    selectionChange(e: StepperSelectionEvent): Promise<void>;
    ngOnInit(): Promise<void>;
    updateModel(): Promise<void>;
}
