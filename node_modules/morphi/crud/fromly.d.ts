import { FormlyFieldConfig } from '@ngx-formly/core';
import { ConfigModels } from 'tnp-config';
export declare function getFromlyConfigFor(target: Function, options?: {
    formType?: ConfigModels.UIFramework;
    keysPathesToInclude?: string[];
    keysPathesToExclude?: string[];
    parentModel?: string;
    relativePath?: string;
    level?: number;
    maxLevel?: number;
}): FormlyFieldConfig[];
export declare type FormlyArrayTransformFn = (fieldsArray: FormlyFieldConfig[], fieldObject?: {
    [propKey: string]: FormlyFieldConfig;
}) => FormlyFieldConfig[];
