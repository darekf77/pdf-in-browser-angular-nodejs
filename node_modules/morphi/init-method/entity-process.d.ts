import * as express from 'express';
import { ModelDataConfig } from '../crud';
export declare class EntityProcess {
    /**
     * Data from backend
     */
    private result;
    private response;
    static init(result: any, response: express.Response, mdc: ModelDataConfig): Promise<void>;
    /**
     * Data to send
     */
    data: any;
    constructor(
    /**
     * Data from backend
     */
    result: any, response: express.Response);
    /**
     * Say yes to:
     * - circural object
     * - transform browser fn in decorator
     */
    private advancedManipulation;
    private checkAdvancedManiupulation;
    private entityMapping;
    private circural;
    private mdc;
    run(mdc: ModelDataConfig): Promise<void>;
    resolveModelDataConfig(): void;
    applayTransformFn(): void;
    setHeaders(): void;
    send(): void;
}
