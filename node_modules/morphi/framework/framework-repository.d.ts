import { Repository as TypeormRepository } from 'typeorm';
export declare function Repository(entity: Function): (target: any) => void;
export declare abstract class BASE_REPOSITORY<Entity, GlobalAliases = {}> extends TypeormRepository<Entity> {
    __?: {
        [prop in keyof GlobalAliases]: {
            [propertyName in keyof Entity]: string;
        };
    };
    _?: GlobalAliases;
    globalAliases?: (keyof GlobalAliases)[];
    pagination(): void;
}
