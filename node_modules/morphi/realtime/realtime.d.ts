import { Socket } from 'socket.io';
import { Server, Namespace } from 'socket.io';
import { FrameworkContext } from '../framework/framework-context';
export declare class RealtimeBase {
    protected context: FrameworkContext;
    private socketFrontEnd;
    private socketFrontEndRealtime;
    private socketNamespaceBE;
    private socketNamespaceBERealtime;
    get socketNamespace(): {
        FE: Socket;
        FE_REALTIME: Socket;
        BE: Server;
        BE_REALTIME: Namespace;
    };
    constructor(context: FrameworkContext);
    pathFor(namespace?: string): URL;
}
